from pyexpat.errors import messages
#
# from flask_wtf import FlaskForm
# from wtforms import StringField
# from wtforms.validators import DataRequired, Email, NumberRange,ValidationError
# from youraoo.model import User
#
# class UserRegistrationForm(FlaskForm):
#     name = StringField('name', validators=[DataRequired()])
#     email = StringField('email', validators=[DataRequired(), Email()])
#     age = StringField('age', validators=[DataRequired(),NumberRange(min=18, message="age must be greater than 18") ])
#
#
#
#     def validate_name(self, field):
#         if User.query.filter(name=field.data).first():
#             raise ValidationError("Name already exists")
#
#

